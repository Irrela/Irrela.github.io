---
layout: post
title: 合并K个升序链表
categories: Algorithm
description: 
keywords: PriorityQueue Comparator
---


```java
    public List<List<Integer>> permute(int[] nums) {
        int len = nums.length;

        List<List<Integer>> res = new ArrayList<>(factorial(len));
        if(len == 0){
            return res;
        }

        Map<Integer, Boolean> isUsed = new HashMap<>();
        Deque<Integer> path = new LinkedList<>();

        dfs(nums, len, 0, path, isUsed, res);

        return res;

    }

    public void dfs(int[] nums, int len, int depth, Deque<Integer> path, Map<Integer, Boolean> isUsed, List<List<Integer>> res){

        if(depth == len){
            res.add(new LinkedList<>(path));
            return;
        }

        for(int i = 0; i < len; i++){
            if(!isUsed.containsKey(i)){
                isUsed.add(i);
                path.addLast(nums[i]);

                dfs(nums, len, depth+1, path, isUsed, res);

                path.removeLast();
                isUsed.remove(i);
            }
        }
    }

    public int factorial(int n){
        int res = 1;

        while(n > 1){
            res *= n;
            n--;
        }

        return res;
    }


```
```java
class Solution {
  public void backtrack(int n,
                        ArrayList<Integer> output,
                        List<List<Integer>> res,
                        int first) {
    // 所有数都填完了
    if (first == n)
      res.add(new ArrayList<Integer>(output));
    for (int i = first; i < n; i++) {
      // 动态维护数组
      Collections.swap(output, first, i);
      // 继续递归填下一个数
      backtrack(n, output, res, first + 1);
      // 撤销操作
      Collections.swap(output, first, i);
    }
  }

  public List<List<Integer>> permute(int[] nums) {
    List<List<Integer>> res = new LinkedList();

    ArrayList<Integer> output = new ArrayList<Integer>();
    for (int num : nums)
      output.add(num);

    int n = nums.length;
    backtrack(n, output, res, 0);
    return res;
  }
}
```